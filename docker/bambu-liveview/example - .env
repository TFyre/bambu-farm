# Copy to `.env` and fix the values (docker compose reads this automatically)
# The domain must resolve (DNS) to this host's public IP
DOMAIN=fixme.example.com
# Email used for Let's Encrypt expiry/renewal notices
EMAIL=fixme@example.com
